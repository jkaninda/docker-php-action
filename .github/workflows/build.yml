name: Docker build

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      docker_tag:
        description: 'Docker tag'
        required: true
        default: 'latest' 
        type: string

env:
  #TAG_NAME: latest
  BUILDKIT_IMAGE: jkaninda/laravel-php-fpm
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - 
        name: Set environment for branch
        run: |
          set -x
          if [[ ${{ inputs.docker_tag }} == 'latest' ]]; then
            echo "TAG_NAME=8.1" >> "$GITHUB_ENV"
            echo "Build latest tag'"
          else
            echo "TAG_NAME=${{ inputs.docker_tag }}" >> "$GITHUB_ENV"
            echo "Build from tag folder'"

          fi
      - 
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - 
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - 
        name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v3
        with:
          context: ./
          file: "./docker/${{env.TAG_NAME}}/Dockerfile"
          push: true
          tags: "${{env.BUILDKIT_IMAGE}}:${{env.TAG_NAME}}"